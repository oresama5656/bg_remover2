Antigravityへの依頼書（アップデート版）
以下のテキストをコピーして、画像（元画像とCanvaの透過済み正解画像）と一緒にAntigravityに渡してください。

【新規開発：高精度AI背景透過ツール 要件定義書】

1. 概要
現在ローカル環境で運用中の「BANANA用スタンプ作成ツール（非ウェブアプリ）」の機能拡張、および将来的な「Webサービスとしての一般公開」を見据えた、高精度の背景透過モジュールの先行開発をお願いします。

2. 現状の課題と目的

課題: 既存のツール（色指定による透過）では、スタンプの「白フチ」「白い文字」「キャラクターの白目や服」など、残すべき白色部分まで一緒に透過されてしまう。

目的: 提供するモデル画像（Canvaで処理したもの）と同等の透過品質を実現すること。被写体（人物、動物、フキダシ内の文字、フチを含むスタンプ全体）を正確に認識し、背景のみを完璧に透過させたい。

3. 技術的アプローチ（推奨）
「特定の色」ではなく「被写体領域」を認識するAI技術を採用してください。

推奨: rembg (Pythonライブラリ)

モデル指定: isnet-general-use または isnet-anime などの「イラストや複数オブジェクト」の認識に強いモデルを必ず指定してください。（※人物特化モデルは、文字や猫が消える原因となるためNGです）

4. 求めるアーキテクチャ（ロジックとUIの分離）
本ツールは、「既存のローカルアプリへの組み込み」と「将来のウェブアプリ化」の両方に対応できるよう、コアロジックとUIを完全に分離（モジュール化またはAPI化） して設計してください。

コアロジック層: 画像パス（またはバイナリ）を受け取り、透過済み画像を返す独立した関数/クラス。将来的な複数画像（フォルダ内画像）の一括バッチ処理を見据え、単一ファイルだけでなくリスト処理も拡張しやすい構造にしてください。

インターフェース層: 今回のフェーズ1では、このロジックの動作検証ができる「単独で動く簡易アプリ」を作成してください。

ドラッグ＆ドロップ（D&D）で画像をアップロードできること。

処理後、PNGとしてダウンロードできること。

※実装例：GradioやStreamlitなどを用いた簡易Web UI、またはFastAPI等のローカルサーバー＋簡易HTMLなど、Webアプリ化の土台になる構成を推奨します。

5. 既存ツールへの統合（フェーズ2の予定）
完成したコアロジック（またはローカルAPI）を、既存の「BANANA用スタンプ作成ツール」に組み込みます。既存ツール側にはラジオボタンを追加し、「従来の色指定透過」と「今回の高精度AI透過」をユーザーが選択できるようにする予定です。まずは単独アプリとしての完成をお願いします。

6. 提供資料

入力用 元画像（背景あり）

正解データ（Canvaで理想的に透過されたPNG画像）

補足：なぜこの指示書でうまくいくのか？
「D&D」と「一括処理」を明記: Antigravityが画面を作るときに、ファイルを複数投げ込めるような枠組みを最初から用意してくれます。

「ロジックとUIの分離」を指示: これを伝えておくことで、後でウェブサイトを作るときに、中身のプログラムをそのままサーバーに持っていくだけで使い回せるようになります（無駄な作り直しが発生しません）。

この内容でAntigravityに依頼を出してみてはいかがでしょうか？ もし調整したい箇所があれば教えてください！